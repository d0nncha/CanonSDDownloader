namespace SDPhotosDownloader;

public static class Downloader
{
    private const string folderFormat = "yyyy";
    public static void Start(string src, string dest, DateTime? date)
    {
        
    }
}